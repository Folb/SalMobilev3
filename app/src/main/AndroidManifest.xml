<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.folb.salmobile">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.MenuActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.TodoActivity" />
        <activity android:name=".activities.lousecounting.LouseCountingActivity" />
        <activity android:name=".activities.menuActivities.CreateAGDActivity" />
        <activity android:name=".activities.menuActivities.CreateDeadFishCountingActivity" />
        <activity android:name=".activities.menuActivities.CreateEnvironmentParamsActivity" />
        <activity android:name=".activities.menuActivities.CreateLouseCountingActivity" />
        <activity android:name=".activities.menuActivities.CreateOwnDefAvtivity" />
        <activity android:name=".activities.menuActivities.SettingsActivity" />
        <activity android:name=".activities.lousecounting.LouseCountingOverviewActivity"></activity>
    </application>

</manifest>